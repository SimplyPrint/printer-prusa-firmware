diff --git a/src/common/marlin_print_preview.cpp b/src/common/marlin_print_preview.cpp
index 7e50cba1452754637f9fc05870ea80c082dfbf22..611b0531d90c6cd7f728d5bbe625234346e1f859 100644
--- a/src/common/marlin_print_preview.cpp
+++ b/src/common/marlin_print_preview.cpp
@@ -532,6 +532,11 @@ PrintPreview::Result PrintPreview::Loop() {
         break;
 
     case State::unfinished_selftest_wait_user:
+        if (skip_if_able == marlin_server::PreviewSkipIfAble::all) {
+            // autoprint (SimplyPrint / one-click / AUTO.GCO): proceed as if Response::Ignore
+            ChangeState(stateFromUpdateCheck());
+            break;
+        }
         switch (response) {
 
         case Response::Ignore:
@@ -555,6 +560,11 @@ PrintPreview::Result PrintPreview::Loop() {
         break;
 
     case State::new_firmware_available_wait_user:
+        if (skip_if_able == marlin_server::PreviewSkipIfAble::all) {
+            // autoprint: proceed as if Response::Continue (also removes the 30 s timeout stall)
+            ChangeState(stateFromPrinterCheck());
+            break;
+        }
         switch (response) {
 
         case Response::Continue:
